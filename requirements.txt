# 疆途·智能巡查管理平台 V1.0 - Python 依赖
# Python 3.11 required

# ─ 核心 ──
requests>=2.31
Pillow>=10.0
numpy>=1.24
pandas>=2.0

# ── GIS ──
geopandas>=0.14
fiona>=1.9
shapely>=2.0
pyproj>=3.6
rasterio>=1.3

# ── GUI / WebView ──
pywebview>=5.0
pythonnet>=3.0

# ── 文档处理 ─
python-docx>=1.0
openpyxl>=3.1
lxml>=4.9

# ── 图像 ──
rawpy>=0.19
exifread>=3.0
piexif>=1.1

# ── 安全 ──
cryptography>=42.0

# ── 打包 ──
pyinstaller>=6.0

# ── Windows 平台 ──
pywin32>=306; sys_platform == "win32"
